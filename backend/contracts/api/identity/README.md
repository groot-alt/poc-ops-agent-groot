# 身份 API 契约

本目录保存内建身份提供方与浏览器登录相关的版本化 API 契约。

首批契约覆盖：

- 用户名密码登录请求；
- 浏览器会话查询响应；
- 首次登录改密请求；
- 管理员受控重置密码请求；
- 身份相关结构化错误响应。

约束：

- 所有契约必须明确认证与授权要求；
- 所有破坏性变更必须通过新版本文件落地；
- 不得把 `local-oidc` 的本地联调语义混入生产身份契约。

{
  "skillId": "certificate-expiry-read",
  "version": "1.0.0",
  "displayName": "证书到期检查",
  "description": "读取指定内部服务端点证书的有效期和剩余天数。",
  "category": "INFRASTRUCTURE_DIAGNOSTICS",
  "riskLevel": "READ_ONLY",
  "executor": "HTTP",
  "outputType": "JSON",
  "readOnly": true,
  "timeoutSeconds": 15,
  "owner": "platform-security",
  "requiredRoles": ["ROLE_ops-reader", "ROLE_ops-admin"],
  "tags": ["certificate", "expiry", "security"],
  "interceptors": ["AUTHORIZATION", "AUDIT"],
  "parameters": [
    {
      "name": "endpoint",
      "displayName": "内部服务端点",
      "description": "需要读取证书有效期的内部 HTTPS 服务端点。",
      "type": "STRING",
      "required": true,
      "allowedValues": [],
      "defaultValue": null
    }
  ]
}
